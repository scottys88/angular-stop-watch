import { InMemoryTimerService } from './in-memory-timer-service';

describe('InMemoryTimerService', () => {
  it('should create an instance', () => {
    expect(new InMemoryTimerService()).toBeTruthy();
  });
});
